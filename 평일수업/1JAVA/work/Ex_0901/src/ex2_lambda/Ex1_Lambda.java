package ex2_lambda;

public class Ex1_Lambda {
	// 람다'식'
	// -메서드를 하나의 표현식(Expression)으로 표기한 것
	// -람다식은 자바 1.8버전부터 도입됨

	// 람다식이 도입된 이유
	// -함수형 프로그래밍 방식
	// -자바에는 함수형 프로그래밍 방식이 적용되지 않았다.
	// -자바는 객체를 기반으로 프로그램을 구현하는 객체지향 프로그램이다.

	// -클래스를 먼저 만들고, 클래스 안에서 객체나 메서드를 정의한다음
	// -클래스를 객체로 만들어야 사용이 가능했다.

	// 자바에서 함수를 호출하는 방법
	// 클래스 -> 함수를 정의 -> 객체를 생성하고 -> 객체를 통해 함수를 호출

	// 다른 프로그래밍 언어(Python, JS등)에서는 함수만 독립적으로 정의하는 게 가능하다.

}
